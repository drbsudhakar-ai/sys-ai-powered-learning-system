import CourseSyllabusWorkspace from "../../../../components/admin/CourseSyllabusWorkspace";
export default function CourseSyllabusPage(){ return <CourseSyllabusWorkspace />; }
CourseSyllabusPage.getLayout = (page) => page;
