import CourseSyllabusWorkspace from "../../../../../components/admin/CourseSyllabusWorkspace";
export default function SyllabusPage() { return <CourseSyllabusWorkspace page="reviews" />; }
SyllabusPage.getLayout = (page) => page;
